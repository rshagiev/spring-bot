# LLM-Driven Crypto Trading Bot

Автоматизированная система исполнения сигналов для Bybit, построенная по плану «LLM-пишет-код».  
Стратегия: человек-трейдер шлёт raw-сигналы → модуль **Spring Model** (Bollinger Bands) решает, брать ли сигнал → бот открывает позицию и жёстко контролирует риск.

---

## 📂 Содержимое репозитория

| Файл | Назначение |
|------|-----------|
| `spring_model.py` | Функция `bounce_prob()` возвращает вероятность отскока (0–1) |
| `backtest_runner.py` | Бэктест + grid-search параметров BB |
| `risk_controls.py` | Декоратор `@check_daily_drawdown` и helper `update_pnl()` |
| `.github/workflows/python-test.yml` | CI: прогоняет `pytest` на каждый PR |
| `tests/*` | Unit-тесты для всех модулей |
| `requirements.txt` | Зависимости (pandas, numpy …) |

---

## ⚙️ Быстрый старт

### 1. Клонируем и создаём окружение

```bash
git clone https://github.com/<YOUR_LOGIN>/spring-bot.git
cd spring-bot
python3 -m venv venv
source venv/bin/activate         # Windows → venv\Scripts\activate
pip install -r requirements.txt

Event-Driven Architecture (EDA)
Идея заключается в том, чтобы не "опрашивать" биржу, а "слушать" её и реагировать на конкретные события.

Как бы это выглядело в идеальном мире:

Размещение ордеров: Выставляем сетку лимитных ордеров на вход и стоп-лосс.
Подписка на события: Подписываемся на WebSocket-поток от Bybit, который присылает нам уведомления: ORDER_FILLED, ORDER_CANCELLED, ORDER_PARTIALLY_FILLED.
Реакция на события:
Пришло событие "заполнился первый ордер на вход": В коде срабатывает колбэк. Он выставляет общий стоп-лосс на всю позицию.
Пришло событие "заполнился N-ый тейк-профит": Срабатывает другой колбэк. Он проверяет, не пора ли двигать стоп в безубыток. Если пора — отправляет команду на редактирование SL-ордера.
Пришло событие "заполнился стоп-лосс": Срабатывает колбэк. Он отменяет все оставшиеся тейк-профит ордера и помечает сделку как закрытую.
Преимущества этого подхода:

Скорость реакции: Реакция на изменение происходит практически мгновенно, а не через 15 секунд ожидания "вотчера". Это критично для HFT, но также полезно и здесь.
Эффективность ресурсов: Нет постоянных REST-запросов к API. WebSocket-соединение гораздо менее ресурсоемкое и не так быстро расходует лимиты API.
Так почему же я предложил "Watcher"?
Потому что в реальном мире чисто событийный подход чрезвычайно хрупок и сложен в реализации. Вот почему "Watcher" (периодическая сверка) является более надежным и отказоустойчивым решением, особенно на начальном этапе.

Проблемы чисто событийного подхода (EDA):
Пропущенные события (The #1 Killer):
Ваш бот перезагрузился.
Пропало интернет-соединение на 20 секунд.
Биржа перезапустила свои WebSocket-серверы.
В вашем коде произошел сбой, и он не успел обработать событие.
Результат: Вы пропустили событие о заполнении тейк-профита. Ваш бот навсегда потерял синхронизацию с реальностью. Он думает, что стоп-лосс все еще на старом месте, хотя его давно пора было передвинуть. Он не знает, что позиция уже частично закрыта. Это фатальная ошибка для логики.
Сложность управления состоянием:
Вам все равно нужна база данных для хранения состояния. При каждом событии вам нужно обновить это состояние. Что если событие пришло, вы начали обновлять БД, и в этот момент бот упал? Вам нужна сложная логика транзакций и восстановления.
"Гонка состояний" (Race Conditions):
Что если вам почти одновременно приходят два события? Например, "ордер частично исполнен" и "ордер отменен пользователем". Порядок их обработки становится критически важным и трудно предсказуемым.
"Единый источник правды":
В событийной модели у вашего бота есть свое "мнение" о состоянии дел, которое он строит на основе потока событий. Но единственный настоящий источник правды — это сама биржа. Если "мнение" вашего бота разошлось с реальностью на бирже, у вас большие проблемы.
"Watcher" как механизм самокоррекции
Подход с периодической сверкой решает все эти проблемы. Его основная философия: "Я не доверяю своему внутреннему состоянию. Я доверяю только бирже".

Каждые 15 секунд "Watcher" выполняет ключевую функцию — самокоррекцию и синхронизацию.

Перезагрузился бот? Не страшно. Через 15 секунд он запустит цикл, получит с биржи реальные открытые позиции и ордера, сравнит с состоянием в своей БД и немедленно исправит любые расхождения. Он увидит, что позиция уже открыта, и выставит стоп, даже если пропустил событие входа.
Пропал интернет? Не страшно. Когда связь восстановится, первый же цикл сверки все исправит.
Случайно закрыли позицию руками на бирже? Не страшно. "Watcher" увидит, что позиции больше нет, и корректно закроет сделку в своей БД, отменив лишние ордера.
Гибридный подход: Лучшее из двух миров (The Ultimate Solution)
В итоге, самые надежные и профессиональные боты используют гибридный подход:

WebSocket для скорости: Они слушают WebSocket-поток для быстрой реакции. Когда приходит событие о заполнении TP, они немедленно пытаются передвинуть SL.
"Watcher" для надежности: Параллельно с этим, каждые 30-60 секунд, запускается "Watcher" (Reconciliation Loop). Его задача — не управлять сделкой, а проверять, что событийная логика не ошиблась. Он сверяет состояние в БД с биржей. Если находит расхождение (например, событие было пропущено и SL не передвинулся), он принудительно исправляет ситуацию.
Почему это лучший вариант:

Вы получаете скорость событийной модели.
Вы получаете железобетонную надежность и отказоустойчивость модели с периодической сверкой. "Watcher" действует как предохранитель и сторожевой таймер.
Вывод для вашего проекта:

Начать с "Watcher"-а — это абсолютно правильный инженерный выбор. Это проще, надежнее и заставит вас с самого начала правильно спроектировать логику работы с состоянием. Когда эта система будет отлажена и стабильно работать, вы можете в качестве оптимизации добавить WebSocket-слушателя для ускорения реакции, оставив "Watcher"-а в качестве гаранта надежности.

Начинать с чисто событийной модели — это прямой путь к трудноуловимым багам и потенциальной потере денег из-за рассинхронизации.